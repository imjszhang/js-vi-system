{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @js-vi/tokens",
    "build:all": "npm run build -w @js-vi/tokens && node build/generate.js",
    "preview": "npx serve . -l 3000 --cors",
    "poster": "npm run poster -w @js-vi/cli"
  }
}
